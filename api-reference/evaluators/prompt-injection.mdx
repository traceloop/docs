---
title: "Prompt Injection"
api: "POST https://api.traceloop.com/v2/evaluators/execute/prompt-injection"
---

Detect prompt injection attempts

## Request Body

<ParamField body="config" type="object">
  <Expandable title="config object">
<ParamField body="threshold" type="number">
  Example: `0.5`
</ParamField>
  </Expandable>
</ParamField>

<ParamField body="input" type="object" required>
  <Expandable title="input object">
<ParamField body="prompt" type="string" required>
  Example: `What is the weather like today?`
</ParamField>
  </Expandable>
</ParamField>

## Response

<ResponseField name="has_injection" type="string">
  Example: `safe`
</ResponseField>

## Example

### Request

```json
{
  "config": {
    "threshold": 0.5
  },
  "input": {
    "prompt": "What is the weather like today?"
  }
}
```

### Response

```json
{
  "has_injection": "safe"
}
```
